import 'package:flutter/material.dart';

class Camera.dart extends StatelessWidget {
  const Camera.dart({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
